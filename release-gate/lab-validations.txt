# perf-sentinel lab validation ledger
# format: <version>\t<lab_commit_sha>\t<YYYY-MM-DD>\t<PASS|FAIL>
# Append-only. Lines are produced by `scripts/record-validation.sh vX.Y.Z PASS` in the perf-sentinel-simulation-lab repo.
v0.6.1	5c041bb	2026-05-09	PASS
v0.7.1	0eeceb4	2026-05-15	PASS
v0.7.2	1cdb2d0	2026-05-15	PASS
v0.7.3	a8cd51d	2026-05-16	PASS
# v0.7.4 PASS excludes Kepler: it shipped with wrong metric names and the lab mock
# mirrored the same wrong assumption. Real-wire conformance came later, via CI.
v0.7.4	c64c7b3	2026-05-21	PASS
v0.7.5	4694046	2026-05-23	PASS
v0.7.6	bb42e85	2026-05-23	PASS
v0.7.7	4a39375	2026-05-27	PASS
v0.7.8	525541f	2026-05-28	PASS
v0.8.0	05f5af4	2026-05-29	PASS
v0.8.1	3da90a0	2026-05-30	PASS
# v0.8.2 candidate 6cf4eab first FAILED verify-hash: content_hash drifted 1 ULP on
# JSON re-parse. Fixed by serde_json float_roundtrip, then re-validated green.
v0.8.2	fa53b74	2026-06-05	PASS
v0.8.3	c5948c6	2026-06-06	PASS
# v0.8.4 was a maintenance image bump, no schema/config/API/metric change: no line.
v0.8.5	97a961b	2026-06-07	PASS
v0.8.6	2cb3bd5	2026-06-08	PASS
v0.8.7	777f5ff	2026-06-10	PASS
v0.8.8	35c96b5	2026-06-11	PASS
v0.8.13	7d3c28a	2026-06-18	PASS
v0.9.0	8ba4bff	2026-06-25	PASS
v0.9.2	7ddd1c2	2026-06-27	PASS
v0.9.3	ee59473	2026-06-29	PASS
v0.9.4	9417ce4	2026-07-01	PASS
v0.9.5	f756744	2026-07-03	PASS
# The v0.9.5 row above tested a binary predating the memory-guard review, whose fixes
# materially changed the guard, so the memory-pressure legs were rerun. The lab tree was
# unchanged, hence the row below carrying product tip 468bf99 to disambiguate.
v0.9.5	468bf99	2026-07-04	PASS
v0.9.6	ebb7354	2026-07-06	PASS
v0.9.7	ad2c2d3	2026-07-10	PASS
v0.9.8	06dd232	2026-07-11	PASS
v0.9.9	c43c8c4	2026-07-12	PASS
v0.9.12	a916bd2	2026-07-17	PASS
v0.9.13	aa4d8c1	2026-07-18	PASS
v0.9.14	33e6a5d	2026-07-19	PASS
v0.9.15	891af5e	2026-07-23	PASS
v0.9.22	caf6e2c	2026-07-27	PASS
v0.9.23	634f839	2026-07-29	PASS
# The v0.9.23 row above tested 8d6847c8. A later review found two defects in sibling
# producer-link resolution, fixed in 0162e6bc, and round 4 revalidated the tagged
# c340424e. The row below is the lab tree that actually attests the tagged revision.
v0.9.23	f9815c9	2026-07-29	PASS
# Scope note: the row above attests c340424e. The later commits on that branch touch no
# ingest, energy or disclosure path and were cleared by CI and inspection, not a lab round.
v0.9.24	df70118	2026-07-31	PASS
# Scope note: the row above attests 57d2a2f9, the tagged 07cb6c6c adding only the version
# bump. Round 3 closed the three round-2 defects and took the gate from 60 to 61 scenarios.
# limit-saturation-curve stays red on the 256 MiB container limit, identically on 0.9.23,
# so it is an environment limit rather than a regression.
v0.9.25	eb90ceb	2026-08-02	PASS
# v0.9.28 closed a silent ingest outage: the OTLP gRPC listener accepted no encoding, so a
# collector left at its gzip default had every batch dropped while the pod stayed ready and
# every counter stayed at zero. The lab had a gRPC path and a compressed path but never both
# at once, hence the new otlp-compression-matrix scenario, every verdict A/B'd against the
# published 0.9.26 image. Scope was narrowed to 37 of 67 scenarios after auditing the
# dependency bumps. Two limits stated rather than buried: limit-saturation-curve stays red on
# the 256 MiB container limit and OOMs at the same point on both images, an environment limit
# confirmed by A/B, and the decompression amplification corner is not exercised by that ramp.
v0.9.28	0aba69d	2026-08-04	PASS
# v0.9.28 delta on the batch acknowledgment path, complementing the row above. The 68th
# scenario, ack-lifecycle-warning, covers the full ack life cycle on real artefacts, its
# blocking guard being that no warning is ever derived from a pre-computed report. Against
# 0.9.26 it fails 5 legs, so it cannot go green on a version lacking the feature. Found
# outside the delta and identical on 0.9.26, therefore not a regression: a ConfigMap-mounted
# ack TOML crashed the daemon at startup (projected keys are symlinks, which the loader
# refuses), fixed on this branch and documented as a subPath mount in HELM-DEPLOYMENT.
v0.9.28	52a3ea3	2026-08-04	PASS
# v0.10.0 ran a scoped validation, not the full scenario sweep: every surface the
# release touches was covered, so the rest was not needed.
v0.10.0	e4f265b	2026-08-06	PASS
# v0.11.0 was scoped to every changed release surface rather than all 69 scenarios. The
# first run found the correlation CSV grouping defect fixed in 61decb60; the focused rerun
# at 05f3ff5f passed grouping 17/17 and Tempo ingestion on the same traces against 0.10.0.
# SKIP_TRAFFIC=1 reused the already-green canonical traffic gate; SKIP_TRIGGER_TEST=1 omitted
# the daemon-down alert trigger because it is outside the 0.11.0 change surface.
v0.11.0	214ea6f	2026-08-07	PASS
# Follow-up attestation for product 10f75892, which only distinguishes Tempo traces with
# no convertible spans. The rebuilt candidate passed batch-tempo-scrape unchanged; every
# other 0.11.0 surface remains covered by the scoped attestation immediately above.
v0.11.0	214ea6f	2026-08-07	PASS
# v0.11.2 was validated against the exact upstream candidate through the RabbitMQ
# multistack rollout: every completed stack passed its historical 10 findings plus both
# messaging findings, and the combined 12-finding runner passed representative Spring
# and Quarkus gates before remaining exhaustive coverage moved to blocking CI.
v0.11.2	0b5b78f	2026-08-09	PASS
v0.12.0	7425309	2026-08-13	PASS
# No entry for v0.13.0, nor for any version tagged until this note is superseded. The
# simulation lab is paused while the hub it runs on is being finished, so those releases
# ship without a lab attestation rather than with a failed one. Nothing was bypassed:
# release.yml runs check-tag-version.sh alone, the lab gate is an operator pre-flight that
# CI never executes. This ledger is append-only and the gate matches the version string
# exactly, so a later run cannot retro-validate them, they stay without a PASS for good.
#
# Superseded 2026-08-14. The hub work reached a point where the lab could run again, so
# the entry below is a real PASS on the seven scoped targets, product revision 4f40cf8d.
# The note above still holds for v0.13.0: append-only means it keeps no attestation.
v0.13.1	aa834b8	2026-08-14	PASS
#
# v0.14.0 has no line: the gate stayed paused while the Hub was being finished,
# as the v0.13.0 note says. Append-only means it keeps no attestation.
#
# v0.15.0 PASS covers 38 of the 79 scenarios, not the whole suite. Scope is what
# this cycle touched: the 0.15.0 surface and schema v1.7, the six new hub-*
# ecosystem scenarios (the first to involve the Hub and the IDE plugin at all),
# every scenario whose assertions were corrected here, and what the version bump
# moved. The untouched families (limit-*, multistack, energy backends,
# resilience) were not re-run. Two scenarios needed a documented
# release-candidate knob rather than a fix: DAEMON_IMAGE for
# chart-disclose-persistence, UPSTREAM_PATH for template-gitlab-ci, both because
# a locally bumped chart and template name a release that is not published yet.
# Validated together: product 3d5a4ae7, which is the chore(release) commit and
# the revision the daemon image was built from. The commits after it touch no
# Rust: the GitLab CI template fix this run surfaced, its CHANGELOG and docs,
# the regenerated dashboard captures, and this ledger line.
# PerfSentinelHub b438f61, JetBrains plugin cc80099. Daemon image
# perf-sentinel:0.15.0-3d5a4ae7, confirmed answering version 0.15.0 on
# /api/status before the run.
v0.15.0	3dc681b	2026-08-27	PASS

# Scoped run: the change touches only the tempo and jaeger-query ingest paths,
# so only the scenarios invoking them ran. batch-tempo-scrape PASS,
# multiformat-input PASS (live Jaeger upstream), batch-victoria-scrape PASS,
# added this run to cover the Jaeger query API against a live Victoria Traces.
# Its middle leg asks for a window ending an hour before the traffic and
# requires an empty result: sending the window as `lookback` ran the search from
# the epoch and returned the same traces, so a findings-only check passes on the
# bug. It also fails on the published image, which rejects --from/--to.
# limit-multi-source left red: it skipped its tempo leg (empty Tempo) and fails
# on filtered{not_io}, the daemon OTLP classifier this release does not touch.
# Not proven pre-existing, the scoped daemon used the local image either way.
# Validated: product c10b035e on feature/0.16.0, image perf-sentinel:0.15.0-c10b035e.
# Cargo.toml is bumped after this gate, so the SHA identifies the content.
v0.16.0	dad0e7c	2026-08-28	PASS
v0.17.0	d7f5889	2026-08-29	PASS
# v0.18.0 PASS is a targeted pass, not verify-all-scenarios in full: the seven
# metric assertions of the per-service label work, 13 priority-1 and 7 priority-2
# scenarios, then a replay of 11 scenarios and validate-findings 12/12 at both
# sanitizer_aware_min_cv 0.5 and 1.0 after the detection change.
v0.18.0	5cd07f7	2026-09-02	PASS
# v0.19.0 PASS is a targeted pass, not verify-all-scenarios in full: the eleven
# metric assertions of the grouping label, eight in-scope scenarios and
# validate-findings 12/12, plus the new grouping-metrics-split scenario that
# freezes the split, the sum invariant, the pair caps and the knob.
v0.19.0	660ceb5	2026-09-03	PASS
# v0.20.0 PASS is a targeted pass, not verify-all-scenarios in full: the new
# incident-window-capture scenario (intake, settle merge, idempotence, counted
# refusals, the NDJSON archive, the read key, until_ms and the last-span gauge),
# appsec-hardening with its read-key leg, and six regression scenarios on the
# touched paths (archive-window-drops, archive-integrity-chain,
# ack-lifecycle-warning, config-fragments, grouping-identity,
# export-snapshot-scope), all against the local release binary. No cluster run:
# the in-cluster [daemon.incidents], the Infinity datasource and the hub-*
# scenarios follow the 0.20.0 image pin.
v0.20.0	c3854bd	2026-09-05	PASS
# v0.20.0 second targeted pass after the namespace label landed on the incident
# intake: incident-window-capture rerun with its two namespace legs (the label
# kept on the record, the read-key filter), against the local release binary.
v0.20.0	c7fbca7	2026-09-05	PASS
# v0.20.1 targeted pass on the scope the patch touches rather than the full
# suite: hub-incidents-mirror end to end against a local binary carrying the
# archive permission fix, 13 verdicts over its six legs. The daemon was first
# put back into the failure the release repairs, an incident archive left at
# mode 660 by an fsGroup mount, and it came up on its own with the file back
# at 0600 where 0.20.0 stayed in CrashLoopBackOff on the same state.
v0.20.1	dadd839	2026-09-05	PASS
# v0.20.2 carries the v0.20.1 lab evidence above, on the same lab commit, and
# runs no pass of its own. Everything this tag adds renders the HTML report:
# the detail cards wrap a long value instead of cutting it at an ellipsis, and
# two rows that keep their single line gained a title. No daemon path moves.
# Five scenarios do load a report in headless Chrome, and none can tell the
# two builds apart: the three ci-e2e ones only judge RENDERED against BLANK
# under a CSP header, and browser-check.sh reads innerText, which is the same
# string whether or not CSS truncated it, without ever opening a finding's
# detail panel where these cards live. The lab holds no reference to
# ps-meta-cell, text-overflow or getComputedStyle.
v0.20.2	dadd839	2026-09-06	PASS
v0.21.0	070998a	2026-09-10	PASS

# v0.22.0, 2026-09-11. Targeted pass, not `verify-all-scenarios`, and the
# reasoning is worth contesting. The runtime diff is 11 lines in
# `check_ack_auth` plus a 4-line `bearer_matches`, already covered by 109 lines
# of unit tests; `ingest/`, `normalize/`, `detect/`, `score/` and the batch
# path are untouched, so most of the suite would re-establish that code
# identical to 0.21.0 still behaves like 0.21.0. What had NO coverage anywhere
# is the other half of the branch: two example files whose alert rules and
# receiver live in Kubernetes, where three of the four links (the CRD schema,
# the operator that renders it, the Alertmanager that delivers it) sit outside
# the product repo. docs/SCENARIOS.md listed "Alertmanager itself" under what
# the lab deliberately does not assert.
#
# So the pass built that coverage instead: a new lab scenario
# `incident-alerting-chain` (11 PASS / 0 FAIL / 1 deliberate SKIP), plus the
# bearer gate asserted on both route families by extending
# `incident-window-capture` and `appsec-hardening`, both re-run green with
# every pre-existing leg. The shipped PrometheusRule was applied byte for byte
# against a real Prometheus, a real Alertmanager delivered with a bearer
# credential, and the released 0.21.0 running beside it under the same
# ConfigMap and the same receiver refused the same delivery at the same
# instant. `make validate` ok, 3508 product tests green.
v0.22.0	05ae8d8	2026-09-11	PASS

# v0.22.1, 2026-09-11. Targeted pass, and narrower than the one above it. The
# diff is nine blocking file operations moved off the runtime workers, so the
# scope is the daemon's write paths and nothing else: no detector, no ingest,
# no scoring, no route, no wire format. The two archive writers keep their
# bodies byte for byte and only change the kind of thread they run on.
#
# Run locally, no cluster, all five green: archive-integrity-chain (the chain
# hashing and the writer), archive-window-drops (rotation and drops),
# incident-window-capture (the incident archive writer), java-ci-capture
# (capture finish) and appsec-hardening (the JSON socket and the ack routes).
# Those cover eight of the nine changed call sites.
#
# NOT exercised, and stated rather than glossed: the socket `remove_file` on
# the daemon shutdown path, which daemon-sigterm-drain owns and which needs a
# cluster. It is the most trivial of the nine, a one-shot unlink of a Unix
# socket during teardown. Operator accepted that gap on 2026-09-11.
v0.22.1	05ae8d8	2026-09-11	PASS

# v0.22.2, 2026-09-15. Targeted pass, not `verify-all-scenarios`, on the model
# of the two rows above. The runtime diff is the consumer-destination
# endpoint fallback in `ingest/` on OTLP, Jaeger and Zipkin, and the
# daemon's TraceWindow retaining consumer destinations across exports with
# their parent links. No detector, no scoring, no route and no wire format
# moves, so the untouched families (limit-*, multistack, energy backends,
# resilience, hub-*) were not re-run.
#
# What ran, all green, against perf-sentinel:0.22.1-8fb14548 built by
# seed-daemon-local from product 8fb14548, the last commit before the
# version bump, and confirmed on every node with k3d-image.sh
# verify_k3d_image_on_all_nodes:
# - endpoint-resolution with its new family F, 53/53 assertions on synthetic
#   CONSUMER roots against the local release binary: template, name and
#   legacy destination keys, the nearest consumer when they nest, a route
#   and a code frame outranking a destination, and every rejected shape
#   (temporary flag as bool and as the string "true", amq.gen-*, <default>
#   in angle brackets, a "?" in the name). The 0.22.1 binary gives 47/53,
#   the six differences being the shapes the release names.
# - consumer-endpoint, new, 4/4 on a real Spring AMQP listener added to
#   notification-service under the OpenTelemetry Java agent 2.30.0: the N+1
#   behind the listener reports `rabbitmq order-service`, the received
#   routing key, instead of `unknown`, on the daemon path, while the two
#   messaging rows of validate-findings keep their HTTP route.
# - correlation-finding: 198 pairs, all above the confidence floor and inside one
#   grouping, the top ones on the new consumer path (the order-service publish
#   ahead of the notification-service N+1).
# - verify-grafana-dashboard with both dashboards resynced from upstream
#   (overview version 8, findings version 5, parity byte for byte after
#   jq --sort-keys), the alert rules, and the new Infinity leg: the lab now
#   provisions the Infinity datasource with the daemon's read key, and the
#   Findings, Correlations, Daemon status and Daemon acknowledgments
#   targets answer through POST /api/ds/query, Findings 200 rows with a `Fix for` of `java jpa` on the
#   listener's N+1, an empty selection with no error, Correlations 206 rows on
#   the 14 columns. The cluster ran kube-prometheus-stack 90.0.0, the Renovate
#   bump to 91.3.0 landed on the lab's main during the pass and templates clean
#   with the same values.
# make validate ok, product fmt, clippy and 3527 tests green.
v0.22.2	f3ea3fe	2026-09-15	PASS

# v0.22.2, second entry, 2026-09-16. Targeted re-validation after the release
# commit: the two Grafana dashboards gained a `Compatibility` badge, the
# `I/O waste ratio` gauge lost the `or vector(0)` that made it paint a green
# zero with nothing to divide, the yellow band moved to `semi-dark-yellow`
# and `rustls` went to 0.23.45 against RUSTSEC-2026-0285. No detector,
# scoring path, route or wire format moved, so only `grafana-dashboard` ran,
# with `SKIP_TRIGGER_TEST=1`.
# Green: both lab dashboards byte-identical to upstream after `jq --sort-keys`
#   (overview 9, findings 6), 29 panels checked and 0 empty, 44 referenced
#   metrics with 0 broken references, the five PrometheusRules loaded, the
#   Infinity leg with Findings 200 rows carrying `Fix for` and Correlations
#   205 rows on the 14 columns, and a new leg running the `Compatibility`
#   badge of each dashboard through Grafana's own datasources: the findings
#   one reads `0.22.1` from `/api/status`, the overview one reads state 0.
# The daemon under test is the published `0.22.1` image, the release changes
#   nothing in the binary beyond the rustls bump, and a daemon older than the
#   branch is what the badges are for.
# Found and fixed in the lab: nothing let Grafana reach Prometheus, so every
#   panel of the overview dashboard timed out from inside the cluster while
#   the scenario kept passing, because its own panel check queries Prometheus
#   over a port-forward. `grafana-prometheus-egress` and
#   `prometheus-allow-grafana` close it, and the new badge leg is the check
#   that would have caught it.
# make validate ok, product fmt and 3527 tests green.
v0.22.2	f4eda0f	2026-09-16	PASS

# v0.23.0, 2026-09-18. Targeted pass at product 8468fd75 (local image
# perf-sentinel:0.22.2-8468fd75 on the three nodes, the tree not yet bumped),
# scoped to the release and to what it can move: the correlator rewrite, the
# cross-batch slow window in the analysis worker, `source_sample_trace_id`,
# the dashboards, local time on the terminal, and clap, clap_complete, ureq
# and toml on their newest patch releases. make validate-findings 12/12, then
# 37 scenarios: every daemon scenario the worker change can reach, the Hub
# and alerting chains, the ack, query, diff and TUI text surfaces, the CSV
# and HTML correlation outputs. Left out as untouched: disclose and
# verify-hash, the crosswalks, the energy backends, the CI templates and
# ci-e2e, the batch ingest formats, the batch limits and network faults.
# New in the lab and green, each with a counter-proof on the published 0.22.2:
#   correlation-event-time: median_lag_ms exactly 5000 and 7000 where 0.22.2
#   reports the tick gap (1501), the earlier event as source although the
#   later one arrived first, both sides' traces opened by /api/explain,
#   window_minutes = 0 refused.
#   slow-window-cross-batch: three one-span episodes 65 s apart yield one
#   slow_sql finding with 3 occurrences on the trace that fired it, nothing
#   at window 0 or on 0.22.2, 61 refused, the key-cap counter exposed.
#   output-formats-coverage 6.F: the text Window line nine hours later under
#   TZ=JST-9 and TZ=Asia/Tokyo in the FROM scratch image, JSON identical.
#   The named zone fell back to UTC silently on 0152910c, which the product
#   fixed in 8468fd75 with an embedded IANA database.
# grafana-dashboard with both dashboards resynced (overview 10, findings 9),
#   Finding type slow_sql 1 row against 200 for All, Correlations 15 rows on
#   15 columns with Source trace and Target trace. correlation-finding 24
#   pairs, all above the floor and inside one grouping, lags 6 s to 42 s
#   where 0.22.2 read 0 (198 pairs there, the confidence fix is the gap).
# long-running-drift as a 5-minute smoke on request: drift -0.18 %.
# limit-prod-window-soak failed once at 10.3 % RSS drift for a 10 % limit.
#   The same soak on 0.22.2 read 5.1 %, max 95.2 MiB, and a rerun of the
#   build under test read 3.0 %, max 95.3 MiB: variance of a 10-minute
#   window, not a leak. correlator_pairs_evicted_total is 195 M on 0.22.2
#   and 336 to 364 M here, tracegen's 64 services on constant timestamps
#   put every finding within the lag threshold of every other.
# Found and fixed in the lab: the incident-alerting-chain A/B twin shares the
#   daemon ConfigMap, whose [daemon.hub_export] seed-hub-local.sh enables, and
#   crashed without the import key. It now mounts the same optional Secret.
# make validate ok, product fmt, clippy and 3557 workspace tests green.
v0.23.0	b26270f	2026-09-18	PASS
#
# v0.24.0, 2026-09-20. Targeted pass, not verify-all-scenarios: the scenarios
# the release touches plus those its detection changes could shift.
# No cluster: diff-mutated-findings (the code-anchor arbitration of the release),
# incident-window-capture (its NDJSON archive, plus the new id leg),
# findings-page-filters, ack-lifecycle-warning, slow-window-cross-batch,
# correlation-event-time, archive-window-drops, output-formats-coverage,
# ci-shift-left, grouping-identity, ruby-activerecord-suggestion, astronomy-shop.
# Cluster: validate-findings 12/12, grafana-dashboard (parity on the version 11
# dashboard and every Infinity target replayed), daemon-ack-workflow (plus the
# new include_toml leg), appsec-hardening, incident-alerting-chain.
# incident-alerting-chain failed first and was lab-side, not product: its twin
# checker keyed every rule on `alert` and its promtool cases published no
# kube_pod_container_info, so they never exercised the new untraced-services
# record. Fixed in the lab, and leg H now reads both groups so a record that
# fails to evaluate cannot leave the alerts silently permissive.
v0.24.0	462d22d	2026-09-20	PASS
#
# v0.25.0, 2026-09-22. Targeted pass, not verify-all-scenarios: no detector
# and no scoring path moved, only the incident freeze and the incidents
# listing. No cluster: incident-window-capture 29/29 against the release
# binary (freeze at reception, settle merge, idempotent reposts, counted
# refusals, NDJSON archive, namespace listing filter, id parameter). Not
# run: incident-alerting-chain, whose rules and receiver are unchanged, and
# grafana-dashboard, the one scenario that replays the Infinity targets of
# the version 12 dashboard's findings=false query.
v0.25.0	dd01136	2026-09-22	PASS
